# 複製這個檔案為 .env，填入金鑰即可執行。
#
# 注意：這支程式的 .env 讀取器很陽春，只認得單純的 KEY=VALUE，
# 不要加引號、不要在值後面接行內註解（# ...），否則會被當成金鑰內容的一部分導致 API 呼叫失敗。
# 要寫註解的話，另起一行、開頭加 # 即可（像這個檔案示範的一樣）。

# 中央氣象署開放資料平台授權碼（兩支程式都需要）
# 這裡預填的是 CWA 官方文件公開的示範金鑰，本身沒有機密性，可以直接用它跑通流程；
# 正式使用請自行到 https://opendata.cwa.gov.tw → 註冊會員 → 會員中心 → API 授權碼 申請專屬金鑰替換。
CWA_API_KEY=rdec-key-123-45678-011121314

# 內部 LLM Gateway（Bedrock-Mantle，OpenAI 相容格式）的 API Key
# 只有 township_wind_rain_etl.py 需要（LLM 合併鄉鎮敘述用）；typhoon_warning_etl.py 不使用 LLM，不需要這把金鑰。
# 這是實際會扣費/計量的金鑰，不預填在這裡——教育訓練當天由講師另外提供，正式環境請換成自己申請的金鑰。
LLM_API_KEY=

# 以下為選填，預設值已對應內部 Bedrock-Mantle 閘道 + google.gemma-4-31b，一般不需要修改
# LLM_BASE_URL=https://bedrock-mantle.us-east-1.api.aws/openai/v1/chat/completions
# LLM_MODEL=google.gemma-4-31b
